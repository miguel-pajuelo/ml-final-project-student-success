# ml-final-project-student-success
End-to-end ML project on student dropout prediction: custom LDA classifier, Gradient Boosting regressor and K-Means clustering built from scratch in NumPy — no sklearn for core algorithms.
